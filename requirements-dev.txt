# Package Requirements
msal[broker]>=1.34,<2 ; platform_system != 'Linux'
msal>=1.34,<2
msal_extensions
questionary
prompt_toolkit>=3.0.41
cachetools>=5.5.0
jmespath
pyyaml==6.0.2
argcomplete>=3.6.2
psutil==7.0.0
requests
cryptography
fabric-cicd>=1.3.0

# Testing and Building Requirements
tox>=4.20.0
pytest>=8.2.1
pytest-cov
vcrpy
build

# Formatting Requirements
# Pinned exactly: Black's stable style changes between releases, so an unpinned
# formatter makes two contributors on different versions reformat each other's
# code indefinitely. These versions MUST match the pins in tox.toml
# ([env.lint] / [env.format]); scripts/check_formatter_pins.py enforces that.
black==26.5.1
isort==8.0.1
# scripts/check_formatter_pins.py parses tox.toml; tomllib is only in the stdlib
# from Python 3.11, and this project supports 3.10.
tomli>=2.0 ; python_version < "3.11"
